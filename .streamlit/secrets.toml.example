# Example Streamlit secrets file (DO NOT commit real secrets)
# Copy this file to `.streamlit/secrets.toml` locally for development
# or add the keys to your Streamlit Cloud app Secrets (App → Settings → Secrets).

# Ramp API Credentials
RAMP_CLIENT_ID = "your_ramp_client_id_here"
RAMP_CLIENT_SECRET = "your_ramp_client_secret_here"

# Azure AD / Microsoft Identity settings for MSAL in-app auth
# Register an app in Azure AD and set the Redirect URI to your Streamlit app URL.
AZURE_CLIENT_ID = "your_azure_client_id_here"
AZURE_CLIENT_SECRET = "your_azure_client_secret_here"
AZURE_TENANT_ID = "your_azure_tenant_id_here"
AZURE_REDIRECT_URI = "https://your-app.streamlit.app/oauth2callback"

# Optional: comma-separated scopes (defaults to openid,profile,email)
AUTH_SCOPES = "openid,profile,email"

# Prevent tampering with cookies (random string). Keep secret in production.
cookie_secret = "replace_with_random_string"

# Notes:
# - Do NOT commit `.streamlit/secrets.toml` with real secrets to source control.
# - On Streamlit Cloud, add these keys in the App's Secrets UI instead of using a file.